@page
@model APMoodle.Pages.BackEnd.UserCreateModel
@{
    ViewData["Title"] = "Create New User";
}

<head>
    <link rel="stylesheet" href="~/css/site.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="~/css/userCreation.css" />
</head>

<form method="post" id="createForm" enctype="multipart/form-data">
    @Html.AntiForgeryToken()
    
    <div class="create-user-container">
        <div class="create-user-header">
            <h1>Create New User</h1>
            <p>Add a new student or lecturer to the system</p>
        </div>

        @if (!string.IsNullOrEmpty(Model.ErrorMessage))
        {
            <div class="alert alert-danger">
                <i class="bi bi-exclamation-circle"></i>
                @Model.ErrorMessage
            </div>
        }

        @if (!string.IsNullOrEmpty(Model.SuccessMessage))
        {
            <div class="alert alert-success">
                <i class="bi bi-check-circle"></i>
                @Model.SuccessMessage
            </div>
        }

        @if (!string.IsNullOrEmpty(Model.InfoMessage))
        {
            <div class="alert alert-info">
                <i class="bi bi-info-circle"></i>
                @Model.InfoMessage
            </div>
        }

        <!-- User Type Selection -->
        <div class="form-group" id="userTypeGroup">
            <label>User Type <span class="required">*</span></label>
            <div class="user-type-selector">
                <div class="user-type-option" data-type="student" onclick="selectUserType('student')">
                    <i class="bi bi-person" style="color: #b8935a;"></i>
                    <div class="type-label">Student</div>
                    <div class="type-desc">Enrolled in courses</div>
                </div>
                <div class="user-type-option" data-type="lecturer" onclick="selectUserType('lecturer')">
                    <i class="bi bi-person-workspace" style="color: #f59e0b;"></i>
                    <div class="type-label">Lecturer</div>
                    <div class="type-desc">Teaches courses</div>
                </div>
            </div>
            <div class="error-message" id="userTypeError">Please select a user type (Student or Lecturer).</div>
            <small class="help-text">Admin accounts cannot be created through this form.</small>
        </div>

        <input type="hidden" asp-for="UserType" id="selectedUserType" />

        <!-- Validation Summary -->
        <div class="validation-summary" id="validationSummary">
            <h4><i class="bi bi-exclamation-circle"></i> Please fix the following errors:</h4>
            <ul id="validationList"></ul>
        </div>

        <!-- Avatar Upload Section -->
        <div class="avatar-upload" id="avatarUpload">
            <div class="preview-container">
                <img src="" alt="Profile" class="avatar-preview" id="avatarPreview" />
                <div class="avatar-placeholder" id="avatarPlaceholder" style="background-color: #78716c;">
                    <span id="avatarInitials">?</span>
                </div>
                <button type="button" class="remove-image-btn" id="removeImageBtn" title="Remove image">
                    <i class="bi bi-x"></i>
                </button>
            </div>
            <div class="avatar-upload-info">
                <label>Profile Picture</label>
                <div class="file-input-wrapper">
                    <span class="file-input-label">
                        <i class="bi bi-upload"></i> Choose Image
                    </span>
                    <input type="file" id="ProfileImage" name="ProfileImage" accept="image/png,image/jpeg,image/jpg,image/gif,image/webp" />
                </div>
                <div class="file-name" id="fileName">
                    <i class="bi bi-info-circle"></i> No file selected
                </div>
                <div class="file-hint">
                    Supported formats: PNG, JPG, JPEG, GIF, WEBP (Max: 5MB)
                </div>
                <button type="button" class="clear-image-btn" id="clearImageBtn">
                    <i class="bi bi-trash"></i> Remove image
                </button>
            </div>
        </div>

        <!-- Personal Information -->
        <div class="form-row">
            <div class="form-group" id="nameGroup">
                <label for="Name">Full Name <span class="required">*</span></label>
                <input type="text" id="Name" asp-for="Name" placeholder="Enter full name" />
                <div class="error-message" id="nameError">Please enter a valid name (at least 2 characters).</div>
                <div class="validation-success" id="nameSuccess"><i class="bi bi-check-circle"></i> Valid</div>
            </div>
            <div class="form-group" id="emailGroup">
                <label for="Email">Email <span class="required">*</span></label>
                <input type="email" id="Email" asp-for="Email" placeholder="Enter email address" />
                <div class="error-message" id="emailError">Please enter a valid email address.</div>
                <div class="validation-success" id="emailSuccess"><i class="bi bi-check-circle"></i> Valid</div>
            </div>
        </div>

        <div class="form-row">
            <div class="form-group" id="phoneGroup">
                <label for="PhoneNumber">Phone Number <span class="required">*</span></label>
                <input type="tel" id="PhoneNumber" asp-for="PhoneNumber" placeholder="e.g., 0123456789" />
                <div class="error-message" id="phoneError">Please enter a valid phone number (10 - 11 digits, starting with 0).</div>
                <div class="validation-success" id="phoneSuccess"><i class="bi bi-check-circle"></i> Valid</div>
            </div>
            <div class="form-group" id="genderGroup">
                <label for="Gender">Gender <span class="required">*</span></label>
                <select id="Gender" asp-for="Gender">
                    <option value="">Select Gender</option>
                    <option value="Male">Male</option>
                    <option value="Female">Female</option>
                    <option value="Other">Other</option>
                </select>
                <div class="error-message" id="genderError">Please select a gender.</div>
                <div class="validation-success" id="genderSuccess"><i class="bi bi-check-circle"></i> Valid</div>
            </div>
        </div>

        <div class="form-row">
            <div class="form-group" id="dobGroup">
                <label for="DOB">Date of Birth <span class="required">*</span></label>
                <input type="date" id="DOB" asp-for="DOB" />
                <div class="error-message" id="dobError">Please select a valid date of birth (must be 15-80 years old).</div>
                <div class="validation-success" id="dobSuccess"><i class="bi bi-check-circle"></i> Valid</div>
            </div>
            <div class="form-group" id="departmentGroup" style="display: none;">
                <label for="Department">Department <span class="required">*</span></label>
                <input type="text" id="Department" asp-for="Department" placeholder="Enter department" />
                <div class="error-message" id="departmentError">Please enter a department.</div>
                <div class="validation-success" id="departmentSuccess"><i class="bi bi-check-circle"></i> Valid</div>
            </div>
        </div>

        <div class="form-actions">
            <button type="button" class="btn-cancel" id="cancelBtn">
                <i class="bi bi-x-circle"></i> Cancel
            </button>
            <button type="submit" class="btn-create" id="createBtn">
                <i class="bi bi-person-plus"></i> Create User
            </button>
        </div>
    </div>
</form>

<!-- Success Modal -->
@if (Model.ShowSuccessModal)
{
    <div id="successModal" class="modal-overlay-full show" onclick="if(event.target===this)this.classList.remove('show')">
        <div class="modal-container">
            <div class="modal-icon success-icon">
                <i class="bi bi-check-circle-fill"></i>
            </div>
            <h3 class="modal-title">Success!</h3>
            <p class="modal-message">@TempData["SuccessMessage"]</p>
            
            @if (!string.IsNullOrEmpty(TempData["GeneratedPassword"]?.ToString()))
            {
                <div style="margin: 16px 0; padding: 14px; background: #fef9e7; border-radius: 10px; border-left: 4px solid #f59e0b; text-align: left;">
                    <p style="margin: 0 0 6px; font-weight: 600; font-size: 14px; color: #1c1917;">
                        <i class="bi bi-key-fill" style="color: #f59e0b;"></i> Generated Password
                    </p>
                    <p style="margin: 0; font-family: monospace; font-size: 18px; letter-spacing: 0.5px; background: white; padding: 10px 14px; border-radius: 6px; border: 1px solid #ece7df;">
                        @TempData["GeneratedPassword"]
                    </p>
                    <p style="font-size: 12px; color: #78716c; margin: 6px 0 0;">
                        <i class="bi bi-info-circle"></i> This password has been sent to the user's email.
                    </p>
                </div>
            }

            <div class="modal-actions">
                <button class="modal-btn modal-success" id="successModalBtn" onclick="window.location.href='/FrontEnd/UserList'">
                    <i class="bi bi-check-circle"></i> Continue
                </button>
            </div>
        </div>
    </div>
}

<!-- Error Modal -->
@if (TempData["ShowErrorModal"] != null && (bool)TempData["ShowErrorModal"])
{
    <div id="errorModal" class="modal-overlay-full show" onclick="if(event.target===this)this.classList.remove('show')">
        <div class="modal-container">
            <div class="modal-icon error-icon">
                <i class="bi bi-exclamation-triangle-fill"></i>
            </div>
            <h3 class="modal-title">Error!</h3>
            <p class="modal-message">@TempData["ErrorMessage"]</p>
            <div class="modal-actions">
                <button class="modal-btn modal-danger" id="errorModalBtn" onclick="document.getElementById('errorModal').classList.remove('show')">
                    <i class="bi bi-check-circle"></i> OK
                </button>
            </div>
        </div>
    </div>
}

<script>
    document.addEventListener('DOMContentLoaded', function() {
        // Get elements
        const fileInput = document.getElementById('ProfileImage');
        const fileName = document.getElementById('fileName');
        const preview = document.getElementById('avatarPreview');
        const placeholder = document.getElementById('avatarPlaceholder');
        const avatarInitials = document.getElementById('avatarInitials');
        const removeImageBtn = document.getElementById('removeImageBtn');
        const clearImageBtn = document.getElementById('clearImageBtn');
        const avatarUpload = document.getElementById('avatarUpload');
        const form = document.getElementById('createForm');
        const createBtn = document.getElementById('createBtn');

        // Form fields for validation
        const userTypeHidden = document.getElementById('selectedUserType');
        const nameInput = document.getElementById('Name');
        const emailInput = document.getElementById('Email');
        const phoneInput = document.getElementById('PhoneNumber');
        const genderSelect = document.getElementById('Gender');
        const dobInput = document.getElementById('DOB');
        const departmentInput = document.getElementById('Department');

        window.selectUserType = function(type) {
            userTypeHidden.value = type;
            
            document.querySelectorAll('.user-type-option').forEach(el => {
                el.classList.remove('selected');
            });
            document.querySelector(`.user-type-option[data-type="${type}"]`).classList.add('selected');
            
            const departmentGroup = document.getElementById('departmentGroup');
            if (type === 'lecturer') {
                departmentGroup.style.display = 'block';
                document.getElementById('Department').required = true;
            } else {
                departmentGroup.style.display = 'none';
                document.getElementById('Department').required = false;
            }
            
            // Update avatar color
            const color = type === 'student' ? '#b8935a' : '#f59e0b';
            document.querySelector('.avatar-placeholder').style.backgroundColor = color;
            
            // Clear validation for user type
            document.getElementById('userTypeGroup').classList.remove('has-error');
            document.getElementById('userTypeError').classList.remove('show');
            
            // Clear validation summary
            document.getElementById('validationSummary').classList.remove('show');
        };

        function validateUserType() {
            const group = document.getElementById('userTypeGroup');
            const error = document.getElementById('userTypeError');
            const value = userTypeHidden.value;
            
            if (!value) {
                group.classList.add('has-error');
                error.classList.add('show');
                return false;
            } else {
                group.classList.remove('has-error');
                error.classList.remove('show');
                return true;
            }
        }

        function validateName() {
            const group = document.getElementById('nameGroup');
            const error = document.getElementById('nameError');
            const success = document.getElementById('nameSuccess');
            const value = nameInput.value.trim();
            
            // Update avatar initials
            if (value.length > 0) {
                const parts = value.split(' ');
                if (parts.length >= 2) {
                    avatarInitials.textContent = (parts[0][0] + parts[1][0]).toUpperCase();
                } else {
                    avatarInitials.textContent = value.substring(0, 2).toUpperCase();
                }
            }
            
            if (value.length < 2) {
                group.classList.add('has-error');
                error.classList.add('show');
                success.classList.remove('show');
                return false;
            } else {
                group.classList.remove('has-error');
                error.classList.remove('show');
                success.classList.add('show');
                return true;
            }
        }

        function validateEmail() {
            const group = document.getElementById('emailGroup');
            const error = document.getElementById('emailError');
            const success = document.getElementById('emailSuccess');
            const value = emailInput.value.trim();
            const emailPattern = /^[^\s@@]+@@[^\s@@]+\.[^\s@@]+$/;
            
            if (!emailPattern.test(value)) {
                group.classList.add('has-error');
                error.classList.add('show');
                success.classList.remove('show');
                return false;
            } else {
                group.classList.remove('has-error');
                error.classList.remove('show');
                success.classList.add('show');
                return true;
            }
        }

        function validatePhone() {
            const group = document.getElementById('phoneGroup');
            const error = document.getElementById('phoneError');
            const success = document.getElementById('phoneSuccess');
            const value = phoneInput.value.trim();
            const phonePattern = /^0\d{9,10}$/;
            
            if (!phonePattern.test(value)) {
                group.classList.add('has-error');
                error.classList.add('show');
                success.classList.remove('show');
                return false;
            } else {
                group.classList.remove('has-error');
                error.classList.remove('show');
                success.classList.add('show');
                return true;
            }
        }

        function validateGender() {
            const group = document.getElementById('genderGroup');
            const error = document.getElementById('genderError');
            const success = document.getElementById('genderSuccess');
            const value = genderSelect.value;
            
            if (!value) {
                group.classList.add('has-error');
                error.classList.add('show');
                success.classList.remove('show');
                return false;
            } else {
                group.classList.remove('has-error');
                error.classList.remove('show');
                success.classList.add('show');
                return true;
            }
        }

        function validateDOB() {
            const group = document.getElementById('dobGroup');
            const error = document.getElementById('dobError');
            const success = document.getElementById('dobSuccess');
            const value = dobInput.value;
            
            if (!value) {
                group.classList.add('has-error');
                error.classList.add('show');
                success.classList.remove('show');
                return false;
            }
            
            const dob = new Date(value);
            const today = new Date();
            let age = today.getFullYear() - dob.getFullYear();
            const monthDiff = today.getMonth() - dob.getMonth();
            if (monthDiff < 0 || (monthDiff === 0 && today.getDate() < dob.getDate())) {
                age--;
            }
            
            if (age < 15 || age > 80) {
                group.classList.add('has-error');
                error.textContent = 'Please select a valid date of birth (must be 15-80 years old).';
                error.classList.add('show');
                success.classList.remove('show');
                return false;
            } else {
                group.classList.remove('has-error');
                error.classList.remove('show');
                success.classList.add('show');
                return true;
            }
        }

        function validateDepartment() {
            if (!departmentInput || departmentInput.style.display === 'none') return true;
            
            const group = document.getElementById('departmentGroup');
            const error = document.getElementById('departmentError');
            const success = document.getElementById('departmentSuccess');
            const value = departmentInput.value.trim();
            
            if (!value) {
                group.classList.add('has-error');
                error.classList.add('show');
                success.classList.remove('show');
                return false;
            } else {
                group.classList.remove('has-error');
                error.classList.remove('show');
                success.classList.add('show');
                return true;
            }
        }

        nameInput.addEventListener('input', validateName);
        nameInput.addEventListener('blur', validateName);

        emailInput.addEventListener('input', validateEmail);
        emailInput.addEventListener('blur', validateEmail);

        phoneInput.addEventListener('input', validatePhone);
        phoneInput.addEventListener('blur', validatePhone);

        genderSelect.addEventListener('change', validateGender);

        dobInput.addEventListener('change', validateDOB);
        dobInput.addEventListener('blur', validateDOB);

        if (departmentInput) {
            departmentInput.addEventListener('input', validateDepartment);
            departmentInput.addEventListener('blur', validateDepartment);
        }

        // ============ FORM VALIDATION ON SUBMIT ============

        form.addEventListener('submit', function(e) {
            // Get current user type from hidden field
            const userType = userTypeHidden.value;

            // Run validations
            const isUserTypeValid = validateUserType();
            const isNameValid = validateName();
            const isEmailValid = validateEmail();
            const isPhoneValid = validatePhone();
            const isGenderValid = validateGender();
            const isDOBValid = validateDOB();
            let isDepartmentValid = true;

            if (userType === 'student') {
                document.getElementById('departmentGroup').classList.remove('has-error');
                document.getElementById('departmentError').classList.remove('show');
                isDepartmentValid = true;
            } else {
                isDepartmentValid = validateDepartment();
            }

            // Collect errors (no password validation)
            const errors = [];
            if (!isUserTypeValid) errors.push('Please select a user type (Student or Lecturer).');
            if (!isNameValid) errors.push('Please enter a valid name (at least 2 characters).');
            if (!isEmailValid) errors.push('Please enter a valid email address.');
            if (!isPhoneValid) errors.push('Please enter a valid phone number (10 - 11 digits, starting with 0).');
            if (!isGenderValid) errors.push('Please select a gender.');
            if (!isDOBValid) errors.push('Please select a valid date of birth (must be 15-80 years old).');
            if (!isDepartmentValid) errors.push('Please enter a department.');

            if (errors.length > 0) {
                e.preventDefault();
                
                // Show validation summary
                const summary = document.getElementById('validationSummary');
                const list = document.getElementById('validationList');
                list.innerHTML = errors.map(err => `<li>${err}</li>`).join('');
                summary.classList.add('show');
                
                // Scroll to the first error
                const firstErrorField = document.querySelector('.form-group.has-error');
                if (firstErrorField) {
                    firstErrorField.scrollIntoView({ behavior: 'smooth', block: 'center' });
                    const input = firstErrorField.querySelector('input, select');
                    if (input) input.focus();
                }
                
                return false;
            }
            
            // Clear validation summary on successful validation
            document.getElementById('validationSummary').classList.remove('show');
            return true;
        });

        // Clear validation summary when user starts typing
        document.querySelectorAll('input, select').forEach(function(element) {
            element.addEventListener('input', function() {
                document.getElementById('validationSummary').classList.remove('show');
            });
            element.addEventListener('change', function() {
                document.getElementById('validationSummary').classList.remove('show');
            });
        });

        // ============ SUCCESS/ERROR MODALS ============

        const successModalBtn = document.getElementById('successModalBtn');
        if (successModalBtn) {
            successModalBtn.addEventListener('click', function() {
                window.location.href = '/FrontEnd/UserList';
            });
        }

        const errorModalBtn = document.getElementById('errorModalBtn');
        if (errorModalBtn) {
            errorModalBtn.addEventListener('click', function() {
                document.getElementById('errorModal').classList.remove('show');
            });
        }

        document.querySelectorAll('.modal-overlay-full').forEach(function(modal) {
            modal.addEventListener('click', function(e) {
                if (e.target === this) {
                    if (this.id === 'successModal') {
                        window.location.href = '/FrontEnd/UserList';
                    } else {
                        this.classList.remove('show');
                    }
                }
            });
        });

        // ============ CANCEL BUTTON ============

        const cancelBtn = document.getElementById('cancelBtn');
        if (cancelBtn) {
            cancelBtn.addEventListener('click', function(e) {
                e.preventDefault();
                showCancelConfirmModal();
            });
        }

        function showCancelConfirmModal() {
            let modal = document.getElementById('cancelConfirmModal');
            if (!modal) {
                modal = document.createElement('div');
                modal.id = 'cancelConfirmModal';
                modal.className = 'modal-overlay-full';
                modal.style.display = 'none';
                modal.innerHTML = `
                    <div class="modal-container">
                        <div class="modal-icon" style="color: #f59e0b;">
                            <i class="bi bi-exclamation-triangle-fill"></i>
                        </div>
                        <h3 class="modal-title">Cancel Creation</h3>
                        <p class="modal-message">Are you sure you want to cancel? All entered data will be lost.</p>
                        <div class="modal-actions">
                            <button class="modal-btn modal-cancel" id="cancelStayBtn">Stay</button>
                            <button class="modal-btn modal-danger" id="cancelLeaveBtn">Leave</button>
                        </div>
                    </div>
                `;
                document.body.appendChild(modal);
            }

            modal.style.display = 'flex';
            modal.classList.add('show');

            document.getElementById('cancelStayBtn').onclick = function() {
                modal.classList.remove('show');
                modal.style.display = 'none';
            };

            document.getElementById('cancelLeaveBtn').onclick = function() {
                modal.classList.remove('show');
                modal.style.display = 'none';
                window.location.href = '/FrontEnd/UserList';
            };

            modal.onclick = function(e) {
                if (e.target === this) {
                    this.classList.remove('show');
                    this.style.display = 'none';
                }
            };
        }

        // ============ AVATAR UPLOAD ============

        function showPreview(imageSrc) {
            preview.src = imageSrc;
            preview.classList.add('show');
            placeholder.classList.add('hide');
            removeImageBtn.classList.add('show');
            clearImageBtn.classList.add('show');
        }

        function hidePreview() {
            preview.src = '';
            preview.classList.remove('show');
            placeholder.classList.remove('hide');
            removeImageBtn.classList.remove('show');
            clearImageBtn.classList.remove('show');
        }

        fileInput.addEventListener('change', function(e) {
            const file = this.files[0];
            if (file) {
                const validTypes = ['image/png', 'image/jpeg', 'image/jpg', 'image/gif', 'image/webp'];
                const fileType = file.type.toLowerCase();
                if (!validTypes.includes(fileType)) {
                    alert('Please select a valid image file (PNG, JPG, JPEG, GIF, or WEBP).');
                    this.value = '';
                    return;
                }

                if (file.size > 5 * 1024 * 1024) {
                    alert('Image size must be less than 5MB.');
                    this.value = '';
                    return;
                }

                const fileSize = (file.size / 1024).toFixed(1);
                fileName.innerHTML = `<i class="bi bi-check-circle" style="color: #22c55e;"></i> ${file.name} (${fileSize} KB)`;

                const reader = new FileReader();
                reader.onload = function(e) {
                    showPreview(e.target.result);
                };
                reader.readAsDataURL(file);
            } else {
                hidePreview();
                fileName.innerHTML = '<i class="bi bi-info-circle"></i> No file selected';
            }
        });

        removeImageBtn.addEventListener('click', function(e) {
            e.stopPropagation();
            hidePreview();
            fileInput.value = '';
            fileName.innerHTML = '<i class="bi bi-info-circle"></i> No file selected';
        });

        clearImageBtn.addEventListener('click', function() {
            hidePreview();
            fileInput.value = '';
            fileName.innerHTML = '<i class="bi bi-info-circle"></i> No file selected';
        });

        avatarUpload.addEventListener('dragover', function(e) {
            e.preventDefault();
            this.classList.add('dragover');
        });

        avatarUpload.addEventListener('dragleave', function(e) {
            e.preventDefault();
            this.classList.remove('dragover');
        });

        avatarUpload.addEventListener('drop', function(e) {
            e.preventDefault();
            this.classList.remove('dragover');
            
            const files = e.dataTransfer.files;
            if (files.length > 0) {
                fileInput.files = files;
                fileInput.dispatchEvent(new Event('change'));
            }
        });
    });
</script>